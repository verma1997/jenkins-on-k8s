pipeline {
    agent {
        label "jenkins-slave"
    }
    stages {
        stage('First Stage') {
            steps {
                echo "This is running in slave agent."
            }
        }
    }
}